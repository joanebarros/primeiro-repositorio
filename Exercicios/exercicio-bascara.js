/**
 * Criem uma função que gera o resultado de uma equação do segundo grau
 * 
 * ax² + bx + c = 0
 * 
 * delta = b² - 4ac
 * b = -b +- raíz de delta / 2a
 */

function calculoDoSegundoGrau (a, b, c) {
    let delta = Math.pow(b, 2) - 4 * a * c

    let raizDelta = Math.sqrt(delta)
     let x1 = (-b + raizDelta)
}

